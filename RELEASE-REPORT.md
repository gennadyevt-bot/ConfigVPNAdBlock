# Config VPN 5.1.6 (37)

Подготовлено 14 сентября 2026 года поверх main a9133d28c67d3d297155158c03cb8f1488f05097 (5.1.5).

## Сборка

- applicationId: com.config.app; minSdk 24; targetSdk/compileSdk 36.
- Gradle Wrapper 9.7.1 с проверкой SHA-256 дистрибутива; Android Gradle Plugin 9.4.0.
- Постоянный сертификат Config VPN: SHA-256 `23b17fcb9f9ec1a78cf9eaa18864cdcc16b580750ddc1da57179cdb53791acb0`.
- Ключ, пароль, local.properties и бинарные сборки исключены из Git. В CI ключ восстанавливается из Secrets во временный каталог и удаляется после сборки.
- CI проверяет debug APK, unit tests и Lint; после успешных проверок main собирает подписанные release APK/AAB, проверяет сертификаты и zipalign, сохраняет SHA-256 и отчёты.

## Исправления

- Возвращены полные проверки Lint с остановкой при ошибках. Исправлены повтор GuideActivity, tint и объявление разрешений.
- Отключён Android backup, сохранён ручной экспорт. Убрано QUERY_ALL_PACKAGES; выбор приложений использует существующий MAIN/LAUNCHER query.
- Добавлены отступы системных панелей для Android 15/16 с сохранением оформления экранов.
- Добавлены объяснения и согласие на VPN, Usage Access и Accessibility. Исправлено преждевременное закрытие окна Domain VPN.
- Убрана запись полных VPN-конфигураций и ссылок/текста Accessibility в новые журналы.
- Настройка собственного сервера проверяет SSH-отпечаток и сохраняет known_hosts; проверяются адрес и порт.
- Изменения 5.1.3–5.1.5, включая DNS и параметры встроенного AmneziaWG-профиля, сохранены.

## Локальная проверка

- assembleRelease, bundleRelease, lintRelease, testDebugUnitTest: успешно.
- 3 unit tests: 0 ошибок, 0 пропусков. Release Lint: 0 ошибок, 228 предупреждений.
- APK: apksigner verify успешно, постоянный сертификат совпадает; zipalign -c -P 16 4 успешно.
- 14 библиотек arm64-v8a/x86_64 имеют ELF LOAD alignment не меньше 16 КБ.
- AAB: bundletool validate успешно; jarsigner сообщает jar verified. Самоподписанный сертификат Android ожидаем; jarsigner также предупреждает о timestamp и различиях чтения ZIP через JarInputStream.

## До отправки в Google Play

- ADB не обнаружил подключённого устройства. Работа VPN, импорт/экспорт, системные разрешения и UI этой версии на телефоне ещё не проверены.
- Встроенный VPN-профиль содержит извлекаемые из APK ключи. Требуется решение владельца о выдаче доступа и ротации ранее опубликованных VPN-учётных данных. Ключ подписи приложения в исходники не добавлен.
- Существующий адрес обновления облачного списка серверов возвращает HTTP 404 без авторизации: репозиторий закрыт. Для функции обновления нужен отдельный подходящий endpoint; нельзя встраивать GitHub-токен в приложение или открывать репозиторий с ключами ради этой функции. Импорт файлов и встроенный профиль сохранены.
- Существующая маршрутизация IPv6 не менялась: нельзя обещать защиту всего IPv6 без теста.
- Нужны публичная политика конфиденциальности, имя разработчика, реальные скриншоты и видео деклараций VpnService/Accessibility/foreground service. Материалы для заполнения находятся в docs/google-play.
- Отправка в Google Play выполняется только после проверки владельцем.
